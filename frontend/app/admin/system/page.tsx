import { SectionPlaceholderPage } from "../../section-placeholder";

export default function AdminSystemPage() {
  return (
    <SectionPlaceholderPage
      eyebrow="Admin"
      title="Sistema"
      description="La route shell di sistema e disponibile per la navigazione e non introduce nuovi controlli o chiamate backend."
    />
  );
}
