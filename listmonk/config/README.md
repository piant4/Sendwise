# listmonk Config

listmonk is the email engine only. It is not the business source of truth and must not decide business logic.

Milestone 0 keeps listmonk configuration minimal. Production deployments must protect listmonk admin access and configure SMTP / Amazon SES only through the approved deployment process.
