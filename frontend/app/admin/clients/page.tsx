import { SectionPlaceholderPage } from "../../section-placeholder";

export default function AdminClientsPage() {
  return (
    <SectionPlaceholderPage
      eyebrow="Admin"
      title="Clienti"
      description="La route shell per i clienti e disponibile e non esegue fetch aggiuntivi in questa milestone."
    />
  );
}
