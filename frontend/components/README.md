# Components

Reusable UI components will live here in later milestones.

Milestone 0 intentionally avoids real dashboard features. Components must call only the FastAPI backend when real API integration begins.
